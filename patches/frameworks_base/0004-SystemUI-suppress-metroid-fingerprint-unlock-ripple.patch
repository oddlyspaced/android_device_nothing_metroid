From 05b7676395460f482038ebd137efc7b3b7d938b4 Mon Sep 17 00:00:00 2001
From: logix727 <logix727@users.noreply.github.com>
Date: Tue, 4 Aug 2026 08:10:20 -0400
Subject: [PATCH 4/5] SystemUI: suppress metroid fingerprint unlock ripple

[metroid/evo] Rebased onto lineage-23.2. Evo/LOS added an "if (!isRippleEnabled)
return" guard at the top of showUnlockedRipple(), which was patch context; it is
preserved ahead of the new showVisualRipple gate so the existing toggle still
short-circuits first.

Dropped the original patch's reindent of the CircleReveal closing paren (20 -> 16
spaces): it is unrelated to the fix and misaligns the block.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RTyThMzCkh5fd3Ct1JP62p
---
 .../systemui/biometrics/AuthRippleController.kt  | 16 ++++++++++------
 1 file changed, 10 insertions(+), 6 deletions(-)

diff --git a/packages/SystemUI/src/com/android/systemui/biometrics/AuthRippleController.kt b/packages/SystemUI/src/com/android/systemui/biometrics/AuthRippleController.kt
index 946088779..9fb68fbbb 100644
--- a/packages/SystemUI/src/com/android/systemui/biometrics/AuthRippleController.kt
+++ b/packages/SystemUI/src/com/android/systemui/biometrics/AuthRippleController.kt
@@ -176,7 +176,7 @@ constructor(
                         ),
                     )
                 logger.showingUnlockRippleAt(it.x, it.y, "FP sensor radius: $udfpsRadius")
-                showUnlockedRipple()
+                showUnlockedRipple(showVisualRipple = false)
             }
         } else if (biometricSourceType == BiometricSourceType.FACE) {
             faceSensorLocation?.let {
@@ -192,16 +192,14 @@ constructor(
                         ),
                     )
                 logger.showingUnlockRippleAt(it.x, it.y, "Face unlock ripple")
-                showUnlockedRipple()
+                showUnlockedRipple(showVisualRipple = true)
             }
         }
     }
 
-    private fun showUnlockedRipple() {
+    private fun showUnlockedRipple(showVisualRipple: Boolean) {
         if (!isRippleEnabled) return
 
-        notificationShadeWindowController.setForcePluginOpen(true, this)
-
         // This code path is not used if the KeyguardTransitionRepository is managing the light
         // reveal scrim.
         if (!ambientAod()) {
@@ -214,6 +212,12 @@ constructor(
             }
         }
 
+        // Nothing's stock SystemUI suppresses the full-screen fingerprint authentication ripple.
+        // Keeping it open through keyguard dismissal leaves black system-bar regions on metroid.
+        if (!showVisualRipple) return
+
+        notificationShadeWindowController.setForcePluginOpen(true, this)
+
         mView.startUnlockedRipple(
             /* end runnable */
             Runnable { notificationShadeWindowController.setForcePluginOpen(false, this) }
@@ -427,7 +431,7 @@ constructor(
                         }
                         pw.println("custom ripple sensorLocation=" + args[1] + ", " + args[2])
                         mView.setSensorLocation(Point(args[1].toInt(), args[2].toInt()))
-                        showUnlockedRipple()
+                        showUnlockedRipple(showVisualRipple = true)
                     }
                     else -> invalidCommand(pw)
                 }
-- 
2.55.0

